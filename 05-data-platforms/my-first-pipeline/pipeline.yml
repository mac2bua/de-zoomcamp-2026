name: bruin-init
schedule: daily
start_date: "2023-03-20"
catchup: false

default_connections:
    duckdb: "duckdb-default"

variables:
  taxi_types:
    type: array
    items:
      type: string
    default: ["yellow", "green"]
